# Test config — Acunetix's intentionally vulnerable test application.
# Explicitly authorized for security scanner testing.
# https://www.acunetix.com/vulnerability-scanner/php-test-site/

client: vulnweb

targets:
  - vulnweb.com

modules:
  - all

output:
  dir: reports/vulnweb
  format: json
  keep_history: true

alerts:
  fail_on: ""
