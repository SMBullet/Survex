# Test config — IBM Altoro Mutual demo banking application.
# Explicitly authorized for security scanner and penetration testing.
# https://demo.testfire.net/

client: testfire

targets:
  - testfire.net

modules:
  - all

output:
  dir: reports/testfire
  format: json
  keep_history: true

alerts:
  fail_on: ""
